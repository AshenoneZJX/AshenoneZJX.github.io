## 目标
- 父组件改为 `MySpace`，路径 `/mySpace`。
- 一级总览命名为 `MySpace`（默认子路由显示总览）。
- 二级页面：`Cars`、`Digital`、`Photography`，分别为 `/mySpace/cars`、`/mySpace/digital`、`/mySpace/photography`。
- `NavBar` 入口指向 `/mySpace`。

## 路由调整
- 更新 `src/router/index.js`：
```js
{
  path: '/mySpace',
  // 父组件：布局容器
  component: MySpace, // src/views/MySpace.vue
  children: [
    { path: '', name: 'MySpace', component: MySpaceOverview },
    { path: 'cars', name: 'Cars', component: Cars },
    { path: 'digital', name: 'Digital', component: Digital },
    { path: 'photography', name: 'Photography', component: Photography },
  ]
}
// 移除原 `/cars` 顶级路由
```
- `NavBar`：将入口从 `/cars` 改为 `/mySpace`。

## 视图文件
- 新增 `src/views/MySpace.vue`：父级布局，包含页头与 `\<router-view />`。
- 新增 `src/views/MySpaceOverview.vue`：一级总览页，三张分类卡片（汽车、数码、摄影），点击跳转到对应二级路由。
- 复用现有 `src/views/Cars.vue` 作为二级 `Cars` 子页（保持当前网格样式）。
- 新增 `src/views/Digital.vue`：二级数码子页（手机/笔记本/配件卡片）。
- 新增 `src/views/Photography.vue`：二级摄影子页（相机/镜头/器材卡片）。

## 交互
- 总览页：三卡片 `router-link` 跳转到 `/mySpace/*` 子页。
- 二级页：页头加入“返回总览”按钮，执行 `this.$router.push('/mySpace')`。
- 动效：沿用 `App.vue` 的 `fade` 过渡动画。

## 风格一致性
- 复用 `App.vue` 中的 `container`（42-47）、`main-content`、以及 `Records.vue` 的 `section-header` 风格（104-111）。
- 网格布局与卡片样式参考当前 `Cars.vue`（32-63 行）。

## 验证
- 访问 `/mySpace` 显示总览；点击进入 `/mySpace/cars|digital|photography`；返回按钮正常返回总览。
- 视觉与交互与现有页面一致，切换时有 `fade` 动画。

## 交付文件
- 修改：`src/router/index.js`（添加 `/mySpace` 嵌套路由，移除旧 `/cars`）
- 修改：`src/components/NavBar.vue`（入口改为 `/mySpace`）
- 新增：`src/views/MySpace.vue`（父布局）
- 新增：`src/views/MySpaceOverview.vue`（一级总览）
- 新增：`src/views/Digital.vue`（二级）
- 新增：`src/views/Photography.vue`（二级）
- 复用：`src/views/Cars.vue`（作为二级 `Cars` 子页）

如果确认，我将基于该方案实施并完成验证。